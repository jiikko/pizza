require 'spec_helper'

describe PizzaClient do
  it 'has a version number' do
    expect(PizzaClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
